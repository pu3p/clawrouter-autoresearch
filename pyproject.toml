[project]
name = "clawrouter-autoresearch"
version = "0.1.0"
description = "Autonomous routing research for ClawRouter using MMLU"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=3.0.0",
    "requests>=2.32.0",
]
